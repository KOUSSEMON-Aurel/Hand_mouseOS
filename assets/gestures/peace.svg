<svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg">
  <style>
    @keyframes flow { to { stroke-dashoffset: -20; } }
    .bone { stroke-dasharray: 4 2; animation: flow 1s linear infinite; }
    .hud-text { font-family: 'Courier New', monospace; font-size: 3px; fill: currentColor; opacity: 0.7; }
    .node { filter: drop-shadow(0 0 2px currentColor); }
  </style>

  <!-- ANGLE ARC HUD -->
  <path d="M40 30 A15 15 0 0 1 60 30" stroke="currentColor" stroke-width="0.5" stroke-dasharray="1 1" opacity="0.6"/>
  <text x="42" y="25" class="hud-text">ANG: 34.5°</text>

  <!-- WRIST -->
  <circle cx="50" cy="85" r="2" class="node" fill="currentColor"/>
  
  <!-- INDEX EXTENDED -->
  <path d="M50 70 L45 45 L35 20" stroke="currentColor" stroke-width="1" class="bone"/>
  <circle cx="35" cy="20" r="2" class="node" fill="currentColor"/>
  <text x="20" y="18" class="hud-text">V_VEC_A[35,20]</text>

  <!-- MIDDLE EXTENDED -->
  <path d="M50 70 L55 45 L65 20" stroke="currentColor" stroke-width="1" class="bone"/>
  <circle cx="65" cy="20" r="2" class="node" fill="currentColor"/>
  <text x="68" y="18" class="hud-text">V_VEC_B[65,20]</text>

  <!-- HUD GLOBAL -->
  <text x="10" y="15" class="hud-text">SEQ: V_GESTURE_MATCH</text>
  <text x="10" y="90" class="hud-text">STABILITY: 0.99</text>
</svg>
